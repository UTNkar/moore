import * as React from "react";
import * as Types from "./types";

declare function SpecialNotice(props: {
  as?: React.ElementType;
  heading?: React.ReactNode;
  text?: React.ReactNode;
}): React.JSX.Element;
